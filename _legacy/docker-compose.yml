version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: parques
      POSTGRES_USER: parques
      POSTGRES_PASSWORD: parques_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
